source ENV['GEM_SOURCE'] || "https://rubygems.org"

group(:development, :test) do
  gem 'yard', :require => false
  gem 'rubocop', "~> 0.26.1"
  gem 'json'
  gem 'redis'
  gem 'rake'
  gem 'minitest'
end
